package ;

class Side {
    public static inline var Right = 0;  // +x
    public static inline var Left = 1;   // -x
    public static inline var Up = 2;     // +y
    public static inline var Down = 3;   // -y
    public static inline var Front = 4;  // +z
    public static inline var Back = 5;   // -z
}